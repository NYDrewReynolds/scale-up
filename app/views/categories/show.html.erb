<div class="page-header">
  <h1>Loan Requests</h1>
</div>

<div class="container">
  <div class="row">
    <div class="col-md-3 pull-left">
      <select class="form-control btn-info">
        <%= @category.each do |category| %>
            <option value="<%= category.title %>"><%= link_to category.title.capitalize, category_path(category) %></option>
        <% end %>
      </select>
    </div>

    <%= render "shared/search" %>

  </div>
</div>

<br>

<%= render "shared/lender_thumbnail" %>
